# 架构设计

> 编写指南：总体保持 50–150 行。每段回答核心问题，不复制代码。

<!-- ═══ 元数据 ═══ -->
- **last_verified**: <!-- YYYY-MM-DD，每次 Phase 2/3 验证后更新。超过 30 天未更新时 Phase 3 发出警告 -->

<!-- ═══════════════════════════════════════════
     核心字段（必填 — 每次变更时必须同步更新）
     ═══════════════════════════════════════════ -->

## 概述
<!-- 2-3 句话：这个程序集是什么？解决什么核心问题？ -->
- **维护者**：<!-- @xxx，团队项目必填，单人项目可省略。维护者拥有本程序集架构与依赖的最终决策权 -->
- **边界模式**: `boundary: hard | soft | shared`<!-- hard=默认，独立模块；soft=同层 feature 间受控共享；shared=公共基础层，任何程序集均可引用 -->

## 核心模型
<!-- 每个关键类/结构 2-3 行：职责、关键字段、与其他模型的关系。
     超过 5 个独立概念时考虑拆分程序集。 -->

## 程序集边界
<!-- 以下四段必填 -->
- **对外暴露**：公共接口与类型列表
- **职责范围**：只负责什么，不负责什么
- **交互方式**：上下游如何调用本程序集
- **边界约束**：本程序集特有的禁止事项

## Contract
<!-- 使用 C# 代码签名格式（只写签名，不写实现），最大化 LLM 代码权重，杜绝参数幻觉。
     修改此段时必须在 history.md 标注 [BREAKING]，并通知所有依赖方。 -->
```csharp
// --- 分组注释 ---
public ReturnType MethodName(ParamType param);  // 所属类，用途简述
```
<!-- Breaking Change 时：
     1. history.md 标注 [BREAKING] + 迁移指引
     2. 在所有依赖方的 links.md 中追加 [NOTICE] 标记 -->

<!-- ═══════════════════════════════════════════
     辅助字段（推荐 — 快速迭代期可延后填写）
     ═══════════════════════════════════════════ -->

## 设计目标
<!-- 3-5 条 bullet；最后一条写「非目标」，明确不做什么 -->
- 目标 1
- 非目标：...

## 性能约束
<!-- 声明本程序集的性能预算。未声明的项视为无特殊约束。
     AI 实现代码时必须在此预算内工作，超标须在设计决策中说明原因。 -->
- **帧预算**：关键方法的每帧耗时上限（如 `UpdateSpot ≤ 2ms`）
- **内存预算**：常驻内存上限 / 对象池容量上限（如 `Chunk 池 ≤ 256`）
- **GC 约束**：热路径零分配 | 允许每帧 N 字节 | 无特殊要求
- **批处理**：单帧处理上限（如 `每帧最多加载 2 个 Chunk`）

## Issues
<!-- 当前模块已知的问题、缺陷或隐患。
     格式：每条用 - 开头，标注严重程度和发现日期。
     已解决的问题移入 lessons.md 并标注修复版本。 -->
<!-- 严重程度：
     🔴 高危 — 触发条件明确，可能造成严重后果
     🟡 需关注 — 有隐患但可控，修改时需额外验证
     🟢 低优先 — 已知不理想，暂不影响正常工作 -->
<!-- 示例：
- 🔴 [2026-03-13] 高并发下 XXX 偶发死锁，暂用 timeout 兜底
- 🟡 [2026-03-10] YYY 接口返回格式与文档不一致，下游已适配但需统一
- 🟢 [2026-03-08] ZZZ 日志级别过高，量大时影响性能
-->

### 危险区域
<!-- 可选。当模块复杂度高或健康等级为 🔴 时填写。
     记录不可轻易触碰的区域、隐藏依赖、跨模块耦合点。 -->
<!-- 示例：
- ⛔ XXX 区域 — 被多处隐式引用，改动会连锁破坏
- ⚠️ YYY 与 ZZZ 有未声明的耦合 — 通过 xxx 方式关联
-->

### 安全操作指南
<!-- 可选。高危模块建议填写。
     明确哪些操作安全、哪些需要额外验证、哪些禁止。 -->
<!-- 示例：
- ✅ 安全：只加新的 xxx，不改旧的
- ⚠️ 危险：修改 xxx 时必须验证条件 1、条件 2
- ⛔ 禁止：除非完整重构，否则不要碰 xxx
-->

### 替代计划
<!-- 可选。如果有计划用新模块替代当前模块，记录在此。 -->
<!-- 示例：
- **替代模块**：NewModule（.dna/ 路径）
- **迁移进度**：30%
- **下一步**：把 xxx 逻辑迁移到新模块
-->

## 设计决策
<!-- 关键技术选型的 Why，格式：决策 → 原因 → 权衡 -->
