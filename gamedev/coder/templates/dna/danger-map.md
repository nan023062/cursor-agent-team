# ⚠️ 高危模块布雷图：[模块名]

> 本模块耦合严重/复杂度极高。**任何修改前必读此文件。**
> 此文件替代标准 architecture.md + pitfalls.md，专用于 🔴 高危模块。

## 模块概况

- **健康等级**：🔴 高危
- **代码量**：~N 行 / N 文件
- **最后一次重构**：YYYY
- **主要维护者**：@xxx
- **一句话描述**：这个模块做什么，为什么危险

---

## 危险地图

### 绝对不要碰的区域
<!-- 列出碰了就会出事的代码区域，说明为什么 -->
- `ClassName.MethodName()` — 原因

### 隐藏依赖（代码里看不出来的）
<!-- 反射调用、字符串路径依赖、隐式约定等 -->
- `ClassA` 被 `ClassB` 通过反射读取 — 改字段名会导致 xxx

### 已知的定时炸弹
<!-- 已知有问题但没有修复的区域，说明触发条件 -->
- 某方法在 xxx 条件下性能崩溃 — 触发条件 / 目前的规避方式

### 跨模块耦合点
<!-- 本模块与哪些模块有不该有的直接依赖 -->
- 与 `ModuleX` 的耦合：通过 xxx 方式 — 影响范围

---

## 安全修改指南

### 安全操作（可以放心做）
<!-- 不会触发连锁反应的修改方式 -->
- 操作 1：只改 xxx，不碰 yyy
- 操作 2：只加新的 xxx，不改旧的

### 危险操作（必须额外验证）
<!-- 可能触发连锁反应的修改，需要额外测试 -->
- 操作：修改 xxx
  - 必须验证：条件 1、条件 2、条件 3
  - 测试设备：至少覆盖 xxx

### 禁止操作（除非完整重构）
<!-- 改了必出事，且无法通过小修补解决 -->
- 禁止 xxx — 原因

---

## 绞杀者计划

<!-- 如果有替代此模块的长期计划，记录在此 -->
- **替代模块**：（如有，模块名 + .dna/ 路径）
- **迁移进度**：N%
- **下一步迁移目标**：把 xxx 逻辑迁移到新模块
- **预计完成**：YYYY-QN
